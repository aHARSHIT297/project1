#include <stdio.h>
#include <stdio.h>
int main()
{
    int n, str[30],i,length;
    printf("Enter the value of string:");
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        length = n - 1;
    }
    printf("The length of string is %d", length);
    return 0;
}